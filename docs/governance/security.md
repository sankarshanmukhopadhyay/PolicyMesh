# Security

PolicyMesh treats network transport, external governance legitimacy, operator identity proofing, third-party registry correctness and signing-key custody as external security boundaries. Production-oriented deployments require TLS, least privilege, protected keys, explicit anchor/signer policy, bounded public surfaces, rate limits, retained audit evidence and monitored reconciliation.

The repository's `SECURITY.md` contains the complete security policy and disclosure guidance.
