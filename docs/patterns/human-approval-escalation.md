# Human approval escalation

Use `DEFER` when an action can become permissible through fresh authority. The approval artifact should bind to the material transaction details and be supplied as new evidence for re-evaluation rather than bypassing policy.

These patterns are illustrative application profiles over the generic Action Decision contract; they are not claims about external legal validity or domain-specific compliance.
