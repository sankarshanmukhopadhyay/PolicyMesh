# Bounded delegated authority

An agent should receive only the actions, resource classes, monetary limits, purposes and validity window required for the task. PolicyMesh treats mandate evidence as an input, not as proof that the mandate is legitimate. Revocation or scope mismatch should fail closed.

These patterns are illustrative application profiles over the generic Action Decision contract; they are not claims about external legal validity or domain-specific compliance.
